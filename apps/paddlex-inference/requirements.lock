# PaddleDetection is intentionally absent: this runtime consumes an exported
# static inference bundle and does not install training plugins.
# PaddlePaddle GPU and the shared PaddleX CV runtime are installed and verified
# before these inference-service dependencies.
fastapi==0.115.14
pillow==11.2.1
python-multipart==0.0.20
uvicorn[standard]==0.34.3
